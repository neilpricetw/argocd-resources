apiVersion: s3.aws.m.upbound.io/v1beta1
kind: Bucket
metadata:
  name: neil-s3bkt-backstage-123
  namespace: default
spec:
  forProvider:
    region: ap-southeast-2
    forceDestroy: true
    tags:
      Environment: dev
      ManagedBy: Crossplane    
  providerConfigRef:
    name: default
    kind: ClusterProviderConfig